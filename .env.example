# GitHub repo URL the "View on GitHub" buttons link to. The landing has no
# hosted chat app — every CTA points at the repo so visitors land on the
# README and clone it. Override this if you fork to a different repo.
NEXT_PUBLIC_GITHUB_URL=https://github.com/juanfiguera/wwxd
